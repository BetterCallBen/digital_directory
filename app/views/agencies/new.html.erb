<% content_for :title, "Nouvelle agence" %>
<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white shadow-lg rounded-lg p-8">
      <h1 class="text-2xl font-bold text-gray-900 mb-6">Ajouter une nouvelle agence</h1>
      <%= render 'form', agency: @agency %>
      <div class="mt-6">
        <%= link_to "← Retour à l'annuaire", agencies_path, class: "text-blue-600 hover:text-blue-800 font-medium" %>
      </div>
    </div>
  </div>
</div>
