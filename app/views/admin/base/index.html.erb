<div class="py-6">
  <div class="border-4 border-dashed border-gray-200 rounded-lg p-8">
    <div class="text-center">
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-3xl font-bold text-gray-900">Dashboard Administration</h1>
        <div class="flex space-x-4">
          <%= link_to "Voir le site", root_path,
              class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors" %>
          <%= link_to "Se déconnecter", destroy_user_session_path, data: { "turbo-method": :delete },
              class: "inline-flex items-center px-4 py-2 border border-transparent rounded-lg text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors" %>
        </div>
      </div>
      <!-- Statistiques -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="p-5">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <span class="h-8 w-8 text-indigo-600">🏢</span>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-500 truncate">Agences</dt>
                  <dd class="text-lg font-medium text-gray-900"><%= @agencies_count %></dd>
                </dl>
              </div>
            </div>
          </div>
        </div>
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="p-5">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <span class="h-8 w-8 text-green-600">🏷️</span>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-500 truncate">Tags</dt>
                  <dd class="text-lg font-medium text-gray-900"><%= @tags_count %></dd>
                </dl>
              </div>
            </div>
          </div>
        </div>
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="p-5">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <span class="h-8 w-8 text-purple-600">👥</span>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-500 truncate">Utilisateurs</dt>
                  <dd class="text-lg font-medium text-gray-900"><%= @users_count %></dd>
                </dl>
              </div>
            </div>
          </div>
        </div>
      </div>
      <!-- Actions rapides -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">Actions rapides</h3>
            <div class="space-y-3">
              <%= link_to "Ajouter une agence", new_admin_agency_path,
                  class: "block w-full bg-indigo-600 hover:bg-indigo-700 text-white text-center py-2 px-4 rounded-md transition-colors" %>
              <%= link_to "Gérer les agences", admin_agencies_path,
                  class: "block w-full bg-gray-600 hover:bg-gray-700 text-white text-center py-2 px-4 rounded-md transition-colors" %>
            </div>
          </div>
        </div>
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">Agences récentes</h3>
            <% if @recent_agencies.any? %>
              <div class="space-y-2">
                <% @recent_agencies.each do |agency| %>
                  <div class="flex justify-between items-center text-sm">
                    <span class="text-gray-900"><%= agency.name %></span>
                    <%= link_to "Voir", admin_agency_path(agency),
                        class: "text-indigo-600 hover:text-indigo-900" %>
                  </div>
                <% end %>
              </div>
            <% else %>
              <p class="text-gray-500 text-sm">Aucune agence récente</p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
