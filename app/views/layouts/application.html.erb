<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) + " - " : "" %>Annuaire Digital</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 min-h-screen">
    <main class="w-full">
      <% if notice %>
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-md mb-4 mx-4 sm:mx-6 lg:mx-8">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-md mb-4 mx-4 sm:mx-6 lg:mx-8">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>
    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-7xl mx-auto px-4 py-8">
        <div class="text-center text-gray-600">
          <p>&copy; <%= Date.current.year %> Annuaire des Agences Digitales</p>
        </div>
      </div>
    </footer>
  </body>
</html>
